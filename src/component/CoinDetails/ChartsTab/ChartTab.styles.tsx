import styled from 'styled-components';
import { Box } from '@mui/material';

export const ChartTabContainer = styled(Box)`
  flex-grow: 1;
  display: flex;
  height: 100%;
  justify-content: center;
  align-items: center;
`;
